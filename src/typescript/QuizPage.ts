import Quiz from "./Quiz";   

/**
 * **Parameters**
 * ```ts
 * let answer: Answer
 * ```
 * - Call when an answer is selected, iterate through adjustments and apply them to the target results.
*/
/*
   applyAnswer(answer: Answer) {
   answer.impacts.forEach(impact => {
         this.results.forEach(result => {
            if(result.text === impact.target) {
               result.value += impact.adjustment;
            }
         });
   });
}*/